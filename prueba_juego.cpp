#include <iostream>
#include "juego.h"
#include "utilidades.h"

/*int main() {
    // Probar tokenizar
    std::vector<std::string> tokens = tokenizar("atacar Ana");
    std::cout << "Tokens: ";
    for (size_t i = 0; i < tokens.size(); i++) {
        std::cout << "[" << tokens[i] << "] ";
    }
    std::cout << std::endl;

    // Probar EstadoJuego
    EstadoJuego estado;
    std::cout << "inicializado = " << estado.inicializado << std::endl;

    Jugador j1;
    j1.nombre = "Ana";
    j1.color = "rojo";
    j1.obtenidoUnidades = false;
    j1.haAtacado = false;
    estado.jugadores.push_back(j1);
    estado.turnos.push_back("Ana");

    std::cout << "existeJugador(Ana) = " << estado.existeJugador("Ana") << std::endl;
    std::cout << "existeJugador(Luis) = " << estado.existeJugador("Luis") << std::endl;
    std::cout << "jugadorActual() = " << estado.jugadorActual() << std::endl;

    return 0;
}*/
